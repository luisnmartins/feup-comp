
module array2
{

  function sum = sum_array(A[])
  {
	i=0;
	sum=0;
	k = A[7];
	io.println(k);
	while(i < A.size) {
		sum=sum + A[i];
		io.print(sum);
		if(i == 10){
			z = 1904;
			sum = sum + z;
		}
		i=i+1;
	}
  }

  function main()
  {

   N=16;
   a=[N];

   i=0;
   while(i < N)
   {
   	a[i] = i;
	x = a[i];
	i=i+1;
	
	io.println(x);
	}

	sum=sum_array(a);
 	io.println("sum of array elements = ",sum);
 	}
}
